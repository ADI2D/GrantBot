# ============================================================================
# GRANTBOT ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# Do NOT commit .env.local to version control
# ============================================================================

# ----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# ----------------------------------------------------------------------------
# Your Supabase project URL (find in Project Settings > API)
SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (safe to expose in browser, find in Project Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (NEVER expose in browser, used for server-side operations)
# This key bypasses Row Level Security - keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----------------------------------------------------------------------------
# DEMO ORGANIZATION (Development)
# ----------------------------------------------------------------------------
# Default organization ID for development/testing
# Replace with a real organization UUID after running seed scripts
NEXT_PUBLIC_DEMO_ORG_ID=8d8d0b4b-8fdc-4d27-92d8-0f4d31e6a001

# ----------------------------------------------------------------------------
# AI SERVICES
# ----------------------------------------------------------------------------
# OpenAI API key for proposal drafting features (currently used)
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Anthropic API key for Claude-powered match scoring and analysis
# Get from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# ----------------------------------------------------------------------------
# STRIPE BILLING (Required for production)
# ----------------------------------------------------------------------------
# Stripe secret key (get from https://dashboard.stripe.com/apikeys)
# Use test key (sk_test_...) for development
STRIPE_SECRET_KEY=sk_test_...

# Stripe webhook secret for verifying webhook signatures
# Get from: stripe listen --forward-to http://localhost:3000/api/stripe/webhook
# Or from Stripe Dashboard > Webhooks for production
STRIPE_WEBHOOK_SECRET=whsec_...

# ----------------------------------------------------------------------------
# APPLICATION URLs
# ----------------------------------------------------------------------------
# Your application's base URL (used for redirects, email links, etc.)
# Development: http://localhost:3000
# Production: https://your-domain.com
APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# CRON JOBS & SCHEDULED TASKS
# ----------------------------------------------------------------------------
# Secret token for authenticating cron job requests
# Generate a random string (e.g., openssl rand -hex 32)
# Set this in Vercel Environment Variables and in your cron configuration
CRON_SECRET=your-random-secret-here

# ----------------------------------------------------------------------------
# EMAIL DELIVERY (Resend)
# ----------------------------------------------------------------------------
# Resend API key for sending proposal share emails
# Get from https://resend.com/api-keys
RESEND_API_KEY=re_...

# Email address to send from (must be verified in Resend)
# Use onboarding@resend.dev for development
RESEND_FROM_EMAIL=onboarding@resend.dev

# ----------------------------------------------------------------------------
# DEVELOPMENT FLAGS
# ----------------------------------------------------------------------------
# Use mock data for Grants.gov connector (useful when API is rate-limited)
# Set to "true" to enable mock mode
GRANTS_GOV_USE_MOCK=false

# ----------------------------------------------------------------------------
# PRODUCTION CHECKLIST
# ----------------------------------------------------------------------------
# Before deploying to production, ensure:
# ✅ All URLs use HTTPS (not HTTP)
# ✅ SUPABASE_SERVICE_ROLE_KEY is kept secret (server-side only)
# ✅ STRIPE_SECRET_KEY uses live key (sk_live_...) not test key
# ✅ STRIPE_WEBHOOK_SECRET is from production webhook endpoint
# ✅ APP_URL matches your deployed domain
# ✅ CRON_SECRET is set in Vercel environment variables
# ✅ RESEND_FROM_EMAIL is verified and uses your domain
# ✅ All API keys are from production accounts
# ============================================================================
